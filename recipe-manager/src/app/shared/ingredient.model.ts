export class Ingredient {

  /*
  name: string;
  amount: number;

  constructor(name: string, amount: number) {
    this.name = name;
    this.amount = amount;
  }
  */

  // Funktioniert auch, dann kann man sich die Deklaration der Eigenschaften sparen
  constructor(public name: string, public amount: number) {

  }


}
